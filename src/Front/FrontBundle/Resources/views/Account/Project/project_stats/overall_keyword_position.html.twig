<div id="container_{{ keyword_overall_for_graph.hash_chart }}" style="min-width: 400px; height: 400px; margin: 0 auto"></div>
<div class="sep"></div>
<script>
        jQuery(document).ready(function(){
            var chart_{{ key }};
            chart_{{ key }} = new Highcharts.Chart({
                chart: {
                    renderTo: 'container_{{ keyword_overall_for_graph.hash_chart }}',
                    type: 'spline',
                    zoomType: 'x'
                },
                title: {
                    text: 'Overall positions of keywords in search engines for {% if graph_period %}{{ graph_period|raw }}{% else %}last 30 days{% endif %} for project <b>"{{ keyword_overall_for_graph.project_name }}"</b>',
                    x: -20 //center
                },
                subtitle: {
                    text: '',
                    x: -20
                },
                xAxis: {
                    categories: {{ keyword_overall_for_graph.xAxis|raw }},
                    overflow: 'undefined',
                    labels: {
                        rotation: -45,
                        aligh: 'right',
                        x: -20,
                        y: 35,
                        style: {
                            fontSize: '11px'
                        }
                    }
                },
                yAxis: {
                    reversed:true,
                    title: {
                        text: 'Position in search engine'
                    },
                    min:1,
                    max:100
                },
                tooltip: {
                    crosshairs: true,
                    shared: true
                },
                plotOptions: {
                    spline: {
                        marker: {
                            radius: 4,
                            lineColor: '#666666',
                            lineWidth: 1
                        }
                    }
                },
                legend: {
                    layout: 'vertical',
                    align: 'right',
                    verticalAlign: 'top',
                    x: -10,
                    y: 50,
                    borderWidth: 0
                },
                series: [
                    {
                        name: 'Google',
                        data: {{ keyword_overall_for_graph.yAxis.Google }}
                    },
                    {
                        name: 'Bing',
                        data: {{ keyword_overall_for_graph.yAxis.Bing }}
                    },
                    {
                        name: 'Yahoo',
                        data: {{ keyword_overall_for_graph.yAxis.Yahoo }}
                    }
                ]
            });
        });
    </script>